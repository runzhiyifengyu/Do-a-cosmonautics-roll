# Do-a-Cosmonautics-roll PRD 检查清单（检查文件）

> 产品需求见 [PRD.md](PRD.md)，开发规则与任务见 [DEVELOPMENT.md](DEVELOPMENT.md)，操作记录见 [OPERATION_LOG.md](OPERATION_LOG.md)。
> 本文件是 PRD 检查清单的**唯一维护位置**（自 DEVELOPMENT.md 第 10 节独立出来），由 AI 维护，随阶段推进更新。
> 每个阶段收尾时，对照 [PRD.md](PRD.md) 逐项检查本清单中对应分组。

## 使用说明

状态标记：

- `[x]` 已通过并记录验证方式
- `[ ]` 未完成
- `[-]` 不适用/已确认不做（仅用于"第一版不做"约束）

全部通过（含用户实际游戏验证）才能标记阶段完成；未通过的项必须记录差异并等待用户确认。

**验收模式**（「验收模式」列）：

- `S`（标准验收模式）：正常单人游戏流程中直接操作与观察验证。
- `D`（Debug 验收模式）：借助调试手段验证——开发日志、调试命令/可视化、构造边界场景（Y=8000 边界、角落、狭窄通道等）、缺失依赖启动测试等，覆盖边界值、状态清理、异常路径与无崩溃。
- `S+D`：两种模式都需执行。Debug 验收所需的具体手段（日志开关、可视化、调试命令）在该项所属阶段实现，未实现前该项不得标记完成。

**Debug 调试功能（阶段 1 已建立基础设施，随阶段扩展）**：

- 开关：游戏内 `/cosmonauticsroll debug on|off|status`（0 级权限，单人可用）；开发环境 `gradlew runClient/runServer` 默认开启；生产默认关闭，可用 `-Dcosmonauticsroll.debug=true` 开启。
- 日志：`Debug.log(...)` 仅在开启时输出，统一 `[Debug]` 前缀，写入 `run/logs/latest.log`。
- 后续阶段按需扩展：状态机日志（阶段 2）、脚部检测与法线日志/可视化（阶段 3）、旋转过程与防抖日志（阶段 4）、楼梯识别日志（阶段 5）、DABR 叠加与降级日志（阶段 6）、重力方向与空间检查日志（阶段 7）、铁把手状态日志（阶段 8）。

---

## 1. 阶段 2 → PRD 3.1 适用区域判断

| 编号 | 检查内容（PRD 原文要点） | 验收模式 | 状态 |
|---|---|---|---|
| 3.1-1 | 实现主世界判断 | S+D | [x] |
| 3.1-2 | 碰撞箱中心 Y 坐标 `>= 8000` 判断 | D | [x] |
| 3.1-3 | 实现 `rocketnautics:deep_space` 维度判断 | S | [x] |
| 3.1-4 | 给其他维度和高度保留后续扩展接口 | S | [x] |
| 3.1-5 | 处理进入、持续处于、离开适用区域三种状态 | S | [x] |
| 3.1-6 | 处理跨越 Y=8000 边界时的状态清理 | D | [x] |
| 3.1-验收 | 低于 8000 不启用；达到 8000 启用；deep_space 启用且不依赖 Y；其他维度不启用；进出区域不残留旧旋转状态 | S+D | [x] |

> **验证记录（2026-08-09，用户测试机游戏内验证 + GitHub Actions 逻辑测试，全部通过）**
>
> **一、逻辑测试覆盖明细**（`src/logictest/.../RegionLogicTest.java`，共 50 个断言，由 Actions `runLogicTests` 执行，输出 50/50 [PASS]）
>
> | 测试方法 | 覆盖内容 | 断言数 |
> |---|---|---|
> | `testOverworldRule` | 主世界 Y=8000 启用 / 8000.001 启用 / 7999.999 不启用 / 0 不启用 / 高负值不启用；下界 8000 不启用、末地 8000 不启用（维度约束） | 8 |
> | `testDeepSpaceRule` | deep_space Y=0 / 负 / 100000 均启用（不依赖 Y）；`rocketnautics:moon` 不启用；主世界不启用 | 5 |
> | `testCompositeRule` | 默认 OR 组合：主世界高空启用、deep_space 启用、主世界低空不启用、其他维度不启用 | 4 |
> | `testStateMachine` | 初始 INACTIVE；进入→ACTIVE+entered；保持无转换事件；deep_space 保持无进入事件（维度内互切）；离开→INACTIVE+left；区域外保持无事件；再次进入 entered；reset 后 INACTIVE 且清空转换标记 | 15 |
> | `testBoundaryCrossing` | 高空 ACTIVE → 跨 7999.9 left=true+INACTIVE → 跨回 8000 entered=true（跨 Y=8000 边界状态清理） | 4 |
> | `testDimensionSwitch` | 主世界高空→deep_space 保持 ACTIVE 无转换事件；→末地 left+INACTIVE；末地→deep_space entered | 5 |
> | `testSurfaceContactTimer` | 接触→CONTACTED；持续接触保持；离开第 1–6 tick 均 RECENTLY_LEFT（0.3s 宽限）；第 7 tick FREE；重新接触归零；reset 归零 | 9 |
>
> **二、游戏内验收步骤表**（S 模式，测试机实际执行，`/cosmonauticsroll debug on` 后观察 `run/logs/latest.log` 的 `[Debug]` 行）
>
> | 步骤 | 操作 | 预期日志 | 结果 |
> |---|---|---|---|
> | 1 | 进入主世界站地面（y<8000） | 心跳 `区域心跳：state=INACTIVE` | ✅ |
> | 2 | `/tp @s ~ 8000 ~`（y≥8000） | `区域 >> 进入：dim=minecraft:overworld y=8000.0 ...`，之后心跳 `state=ACTIVE` | ✅ |
> | 3 | 降回 y<8000 | `区域 << 离开：...`，之后心跳 `state=INACTIVE` | ✅ |
> | 4 | 传送至 deep_space 任意 y（如 y=100） | 心跳 `state=ACTIVE`（不依赖 Y） | ✅ |
> | 5 | 传送至末地 / 下界 | 心跳 `state=INACTIVE` | ✅ |
> | 6 | deep_space 直接传送回主世界 y=9000 | 先 `区域 reset：... reason=维度切换`，再 `区域 >> 进入`（无残留状态） | ✅ |
> | 7 | 死亡重生 | `区域 reset：... reason=死亡重生` | ✅ |
> | 8 | `/cosmonauticsroll debug off` | `[Debug]` 日志停止输出（确认调试开关不影响正常游戏） | ✅ |
>
> **三、审查中额外确认的边界情况**
> - 0.3 秒计时：`LEAVE_GRACE_TICKS=6`（20 tick/s），离开第 1–6 tick 为 `RECENTLY_LEFT`、第 7 tick（0.35s）进入 `FREE`，与 PRD 2.3「约 0.3 秒」一致（测试 9 断言覆盖）。
> - 状态机转换标记生命周期：`entered()/left()` 在每次 `update()` 开头清零、只反映本次 update，防止跨 tick 误触发旋转初始化/清理。
> - 维度互切（主世界高空↔deep_space）不产生 entered/left 事件：两者同属 ACTIVE，仅在真正进出适用区域时触发，避免假转换。
> - debug 关闭时 `RegionDebugTicker` 零副作用（开头直接 return，不创建状态机实例、不输出日志）；玩家登出时清理实例防内存堆积。
> - `RegionDebugTicker` 仅用于 Debug 验收观察；阶段 3 起正式脚部检测/旋转逻辑自挂 ticker，与验收工具解耦（见 DEVELOPMENT.md 阶段 3 实现说明）。

---

## 2. 阶段 3 → PRD 3.2 脚部表面检测

| 编号 | 检查内容（PRD 原文要点） | 验收模式 | 状态 |
|---|---|---|---|
| 3.2-1 | 定义跟随玩家身体方向变化的脚部检测区域 | D | [x] |
| 3.2-2 | 只检测脚部，不使用头部或身体其他部位作为站立依据 | D | [x] |
| 3.2-3 | 获取脚部接触的方块碰撞面法线 | D | [x] |
| 3.2-4 | 合并同一方向的多个方块接触 | S | [x] |
| 3.2-5 | 区分单一方向表面和多个不同方向表面 | S+D | [x] |
| 3.2-6 | 为地面、墙面、天花板提供统一方向表示 | S | [x] |
| 3.2-7 | 处理边缘、角落、斜面和楼梯碰撞形状 | D | [x] |
| 3.2-验收 | 单方向表面可站立；多方向表面不错误站立；身体旋转后脚部检测跟随旋转；身体其他部位接触不单独触发站立 | S+D | [x] |

> **验证记录（2026-08-10，用户游戏内验收 + GitHub Actions 逻辑测试）**
>
> **一、逻辑测试覆盖明细**（`src/logictest/.../FootSurfaceLogicTest.java`，46 断言，由 Actions `runLogicTests` 执行，总计 96/96 [PASS]）
>
> | 测试方法 | 覆盖内容 |
> |---|---|
> | `testSinglePointLayout` | 单点布局 1 个采样点、矩形布局 5 点、半宽/半深参数生效 |
> | `testWorldOffsetRotation` | 采样点随 bodyUp/bodyForward 旋转，身体旋转后局部点映射到正确世界坐标 |
> | `testNoSurface` | 无接触 → NONE |
> | `testSingleSurface` | 单方向 → SINGLE 且法线正确 |
> | `testMultipleSurfaces` | 两个不同方向 → MULTIPLE（不错误站立） |
> | `testDeduplicateSameDirection` | 同方向多次接触合并为单一方向（3.2-4） |
> | `testQueryOffset` | 下沉 0.1 进入表面内查询命中 |
>
> **二、游戏内验收步骤表**（S 模式，测试机实际执行，`/cosmonauticsroll debug on` 后观察 `run/logs/latest.log` 的 `[Debug]` 行；物理化方块为 Sable 子世界，走 `source=sublevel` 路径）
>
> | 步骤 | 操作 | 预期日志 | 结果 |
> |---|---|---|---|
> | 1 | 站水平物理化木板（y=8000） | `脚部检测：result=SINGLE((-0.001,1.000,-0.000)) source=sublevel`，bodyUp 竖直 | ✅ |
> | 2 | 站侧立物理化木板（侧面朝上） | `脚部检测：result=SINGLE(0.985,0.060,0.158) source=sublevel` → **法线跟随表面方向（近似水平）** | ✅ |
> | 3 | 脚部采样点日志 | `block=air/void_air`、`collisionEmpty=true`（物理化方块非普通方块，靠 sublevel 路径命中，正常） | ✅ |
> | 4 | 检查一次性警告 | 无 `Sable 不可用`/`部分 API 不可用`/`getTrackingSubLevel 返回 null`/`包围盒查询未命中` | ✅ |
>
> **三、已知边界说明（已记录，非阻塞）**
> - **Sable（物理化方块）路径当前只返回单一方向**：`RegionDebugTicker` 组合逻辑在 `source=sublevel` 时直接取第一个命中的子世界方向，不产生 MULTIPLE；物理化方块的多方向（墙角）检测是**已知盲区**，随阶段 4 增强（每个采样点单独查询子世界方向后合并）。**已解决**：阶段 4 通过 `SableSubLevelDetector.detectStandingDirections` 收集脚底薄片内所有相交子世界方向，物理化墙角现可产生 MULTIPLE（见第 3 节验证记录，2026-08-11 游戏内确认）。
> - 普通方块路径的 MULTIPLE 合并（5 采样点去重）已有单元测试覆盖；"身体其他部位接触不触发站立"由"只采样脚底矩形"保证（采样点均在脚底平面 down=0）。
> - 阶段 3 验收范围按 A 方案收尾：核心验收项（单方向可站立 + 方向跟随旋转）游戏内通过，多方向增强列入阶段 4 待办。
> - 楼梯/斜面：阶段 5 用专用楼梯逻辑处理，本阶段碰撞形状查询对斜面保守返回最近轴向面（可能 MULTIPLE 不站立，符合"不错误站立"目标）。

---

## 3. 阶段 4 → PRD 3.3 平滑旋转

| 编号 | 检查内容（PRD 原文要点） | 验收模式 | 状态 |
|---|---|---|---|
| 3.3-1 | 实现站立方向的平滑旋转过程 | S | [x] |
| 3.3-2 | 允许地面、墙面、天花板之间进行方向过渡 | S | [x] |
| 3.3-3 | 防止方向在边缘位置反复抖动 | D | [x] |
| 3.3-4 | 处理玩家快速移动和快速离开表面的情况 | D | [x] |
| 3.3-5 | 旋转过程中保留 Do a Barrel Roll 的原有翻滚姿态 | S | [x] |
| 3.3-6 | 明确本模组旋转与 Do a Barrel Roll 旋转的叠加顺序 | D | [x] |
| 3.3-7 | 离开适用区域时平滑恢复原版竖直方向 | S | [x] |
| 3.3-验收 | 身体转向不瞬间跳变；地面→墙面平滑约 90 度；方向不持续来回切换；离开区域恢复朝向；DABR 翻滚不被覆盖 | S+D | [x] |

> **验证记录（2026-08-11，用户游戏内验收 + GitHub Actions 逻辑测试，全部通过）**
>
> **一、逻辑测试覆盖明细**（`src/logictest/.../RotationLogicTest.java`，8 用例 30+ 断言，由 Actions `runLogicTests` 执行，总计 126+ [PASS]）
>
> | 测试方法 | 覆盖内容 |
> |---|---|
> | `testNoInstantJump` | 单 tick 旋转角 <= 每 tick 最大角（不瞬间跳变，3.3-1） |
> | `testGroundToWallSmooth` | 地面→墙面平滑约 90°，步长连续、最终到达、耗时合理（3.3-2/验收） |
> | `testCeilingTransition` | 地面→墙面→天花板（DOWN）连续过渡（3.3-2） |
> | `testAntiShakeSwitchLock` | 切换锁：锁定期内 >90° 方向变化被忽略，锁定期后允许切换（3.3-3） |
> | `testAntiShakeMultipleKeepsCurrent` | MULTIPLE（墙角）保持当前方向不跳转（3.3-3） |
> | `testFastLeaveKeepsDirection` | 快速离开表面（NONE）保持最后方向，不瞬间跳回竖直（3.3-4） |
> | `testLeaveRegionRestoresVertical` | 离开区域平滑恢复竖直，非瞬间跳变（3.3-7） |
> | `testReset` / `testStandingDirectionState` / `testSmootherMath` | 重置立即竖直；SINGLE 更新/NONE·MULTIPLE 保持；slerp/夹角/参数校验/死区 |
>
> **二、游戏内验收步骤表**（D 模式日志验收，测试机实际执行，`/cosmonauticsroll debug on` 后观察 `run/logs/latest.log` 的 `[Debug]` 行；玩家身体不转，观察 `current` 数值渐变，物理化方块走 `source=sublevel` 路径）
>
> | 步骤 | 操作 | 预期日志 | 结果 |
> |---|---|---|---|
> | 1 | 站水平物理化木板（y≥8000） | `旋转：current=(0.000,1.000,0.000) target=(0.000,1.000,0.000)`（方向一致） | ✅ |
> | 2 | 站侧立物理化木板（侧面朝上） | `旋转：current=(0.000,1.000,0.000) target=(0.985,0.060,0.158)` → current 每 0.5s **逐步渐变**至 `(0.985,...)`，无跳变（**平滑过渡可见，3.3-1 验收**） | ✅ |
> | 3 | 跳开悬空 | `result=NONE`，target 保持最后方向，current 不动（快速离开不瞬间跳回竖直，3.3-4） | ✅ |
> | 4 | `/tp @s ~ 7000 ~` 降回 y<8000 | `旋转 << 离开：... 开始平滑恢复竖直` → current 从 `(0.985,...)` **逐步**回到 `(0.000,1.000,0.000)`（**恢复竖直平滑，3.3-7 验收**） | ✅ |
> | 5 | 站两个不同朝向物理化方块交界（墙角） | `result=MULTIPLE source=sublevel`（**Sable 多方向增强生效**）+ 方向保持（3.3-3） | ✅ |
>
> **三、审查中额外确认的边界情况**
> - **平滑机制**：`RotationSmoother` slerp 插值 + 每 tick 最大旋转角 4°（20 tick/s 约每秒 80°），90° 过渡约 22 tick（1.1 秒），吸附阈值 0.5° 消除残余抖动；三重防抖：目标死区（<0.25° 忽略）+ 切换锁（4 tick 内 >90° 变化忽略）+ MULTIPLE 保持当前。
> - **DABR 叠加顺序（3.3-6）已明确并文档化**：本模组表面站立方向作为**基础姿态旋转**，DABR 俯仰/偏航/翻滚在其上**叠加**，本模组不写入/覆盖 DABR 的翻滚值；阶段 4 起即不修改玩家 yRot/pitch/roll，从架构上保证不覆盖 DABR（3.3-5）。
> - **阶段 4 验收范围说明（差异记录，非阻塞）**：本阶段只计算平滑方向并输出日志（`旋转：current=... target=...`），**尚未把方向应用到玩家身体/视角**（S 模式身体实际旋转体验随阶段 6 接入 DABR 时落地）；普通方块墙面需身体旋转后检测区域跟随才能测到，本阶段以物理化方块（Sable 子世界路径不依赖身体方向）+ 日志数值渐变完成平滑/防抖/恢复竖直的验收。
> - **Sable 多方向盲区已解决**：阶段 3 记录的"物理化墙角只返回单一方向"盲区，本阶段通过 `SableSubLevelDetector.detectStandingDirections`（收集脚底薄片内所有相交子世界方向）修复，游戏内墙角已见 `MULTIPLE`。
> - `RotationTicker`（正式旋转逻辑）无条件注册、每玩家状态实例、离开区域/维度切换/死亡重生清理，与 `RegionDebugTicker`（调试观察）解耦；`FootSurfaceResolver` 为两者共用的组合检测实现。

---

## 4. 阶段 5 → PRD 3.4 楼梯旋转

| 编号 | 检查内容（PRD 原文要点） | 验收模式 | 状态 |
|---|---|---|---|
| 3.4-1 | 识别原版和模组楼梯 | S+D | [ ] |
| 3.4-2 | 根据楼梯碰撞形状判断玩家当前行走进度 | D | [ ] |
| 3.4-3 | 根据楼梯方向和高度连续调整玩家身体角度 | S | [ ] |
| 3.4-4 | 区分正常上楼和进入墙面的情况 | S+D | [ ] |
| 3.4-5 | 防止楼梯边缘造成旋转抖动 | D | [ ] |
| 3.4-6 | 处理上下楼、倒退和横向移动 | D | [ ] |
| 3.4-验收 | 原版/模组楼梯正常行走；上楼角度连续无突然翻转；普通完整方块仍用普通表面判断；半砖、活板门不当作楼梯 | S+D | [ ] |

---

## 5. 阶段 6 → PRD 3.5 Do a Barrel Roll 兼容

| 编号 | 检查内容（PRD 原文要点） | 验收模式 | 状态 |
|---|---|---|---|
| 3.5-1 | 使用 Mixin 或其他必要兼容入口接入 Do a Barrel Roll | D | [ ] |
| 3.5-2 | 确认实际注入点来自对应版本源码或映射 | D | [ ] |
| 3.5-3 | 保留 Do a Barrel Roll 的俯仰、偏航和翻滚效果 | S | [ ] |
| 3.5-4 | 只叠加本模组的表面方向和铁把手方向 | D | [ ] |
| 3.5-5 | 避免重复修改同一个旋转值 | D | [ ] |
| 3.5-6 | 处理模组未安装、版本不匹配或相关类不可用的情况 | D | [ ] |
| 3.5-验收 | DABR 翻滚仍可用；本模组不覆盖其翻滚姿态；旋转组合无明显跳变/反向/异常锁定；缺少可选兼容内容不导致启动崩溃 | S+D | [ ] |

---

## 6. 阶段 7 → PRD 3.6 旋转碰撞和防穿模

| 编号 | 检查内容（PRD 原文要点） | 验收模式 | 状态 |
|---|---|---|---|
| 3.6-1 | 不依赖外部 Gravity API，方向重力自研（Mixin `LivingEntity.travel()`，重力方向旋转到表面法线反方向） | D | [ ] |
| 3.6-2 | 重力大小由 Cosmonautics/Sable 决定，本模组只改变重力方向 | D | [ ] |
| 3.6-3 | 最终发布包为单个模组 JAR（无内嵌依赖） | D | [ ] |
| 3.6-4 | 处理旋转后的玩家碰撞方向 | S | [ ] |
| 3.6-5 | 旋转前检查目标空间是否足够 | D | [ ] |
| 3.6-6 | 空间不足时限制或暂缓旋转 | D | [ ] |
| 3.6-7 | 不使用缩放模型来解决穿模 | S | [ ] |
| 3.6-8 | 处理第一人称镜头 | S | [ ] |
| 3.6-9 | 处理第三人称玩家模型 | S | [ ] |
| 3.6-10 | 处理旁观或其他观察视角下的玩家模型 | S | [ ] |
| 3.6-11 | 处理旋转时靠近方块、墙角和狭窄通道的情况 | D | [ ] |
| 3.6-验收 | 旋转不强制送进方块；空间不足限制/暂停；第一人称不明显穿入方块；第三人称模型方向与碰撞方向一致；不异常弹飞或卡死 | S+D | [ ] |

---

## 7. 阶段 8 → PRD 3.7 铁把手方向对齐

| 编号 | 检查内容（PRD 原文要点） | 验收模式 | 状态 |
|---|---|---|---|
| 3.7-1 | 识别 Aeronautics 铁把手 | S | [ ] |
| 3.7-2 | 读取铁把手长轴方向 | D | [ ] |
| 3.7-3 | 在铁把手原有拉近逻辑执行时叠加玩家身体旋转 | S | [ ] |
| 3.7-4 | 使身体纵轴与铁把手长轴平行 | S | [ ] |
| 3.7-5 | 根据玩家当前位置自动确定头脚方向 | S+D | [ ] |
| 3.7-6 | 处理铁把手状态结束 | S | [ ] |
| 3.7-7 | 防止铁把手旋转与表面站立旋转互相抢占 | D | [ ] |
| 3.7-验收 | 右键铁把手原有行为未改变；拉近后身体与铁把手平行；头脚方向自动选择；状态结束后恢复正常旋转 | S+D | [ ] |

---

## 8. 第一版不做的内容（PRD 第 4 节约束）

> 这些内容第一版**不实现**，仅在阶段 9 复查确认确实未实现、未引入相关依赖。

| 编号 | 内容（PRD 原文要点） | 验收模式 | 状态 |
|---|---|---|---|
| 4-1 | 多人联机支持 | D | [-] |
| 4-2 | 服务端旋转状态同步 | D | [-] |
| 4-3 | 服务端碰撞和反作弊校验 | D | [-] |
| 4-4 | 跨玩家同步身体姿态 | D | [-] |
| 4-5 | 其他维度的完整功能 | D | [-] |
| 4-6 | 主世界 Y 小于 8000 时的完整功能 | D | [-] |
| 4-7 | 重力大小计算 | D | [-] |
| 4-8 | 无重力判定 | D | [-] |
| 4-9 | 修改其他模组的重力系统 | D | [-] |
| 4-10 | 修改 Do a Barrel Roll 的核心翻滚算法 | D | [-] |
| 4-11 | 修改 Aeronautics 铁把手的右键、拉近和移动逻辑 | D | [-] |
| 4-12 | 半砖、活板门、普通斜面等非楼梯方块的楼梯式处理 | D | [-] |
| 4-13 | 复杂载具、骑乘实体和其他特殊实体的旋转适配 | D | [-] |
| 4-14 | 服务端持久化玩家旋转数据 | D | [-] |
| 4-15 | 配置界面 | D | [-] |
| 4-16 | 自定义按键 | D | [-] |
| 4-17 | 多人网络协议 | D | [-] |
| 4-18 | 对所有其他旋转类模组的兼容 | D | [-] |

---

## 9. 全局验收（PRD 第 5 节 + DEVELOPMENT.md 第 7 节，阶段 9 复查）

> 阶段 9 复查时逐项勾选。当前已由阶段 2/3/4 验收覆盖：**5-1/5-2/5-3**（阶段 2 游戏内验收，见第 1 节）、**5-5/5-6**（阶段 3 逻辑测试 + 游戏内验收，见第 2 节）、**5-7/5-12/5-18**（阶段 4 D 模式日志验收：平滑过渡/恢复竖直/墙角防抖，见第 3 节；S 模式身体实际旋转随阶段 6 补验）、**7-1/7-2/7-5**（阶段 1 已验证）。其余项待对应阶段完成。

| 编号 | 检查内容 | 验收模式 | 状态 |
|---|---|---|---|
| 5-1 | 主世界中心 Y 达到 8000 时启用 | S+D | [ ] |
| 5-2 | `rocketnautics:deep_space` 中启用 | S | [ ] |
| 5-3 | 其他维度和主世界低于 8000 时不启用完整功能 | S+D | [ ] |
| 5-4 | 离开方块约 0.3 秒后进入对应旋转状态 | D | [ ] |
| 5-5 | 脚部只接触单一方向表面时可以站立 | S | [ ] |
| 5-6 | 脚部接触多个不同方向表面时不会站立 | S+D | [ ] |
| 5-7 | 地面、墙面和天花板之间可以平滑转向 | S | [ ] |
| 5-8 | 原版和模组楼梯都可以按楼梯逻辑行走 | S+D | [ ] |
| 5-9 | Do a Barrel Roll 原有翻滚功能保持不变 | S | [ ] |
| 5-10 | 铁把手右键和拉近逻辑保持不变 | S | [ ] |
| 5-11 | 铁把手状态下身体与铁把手长轴平行 | S | [ ] |
| 5-12 | 离开适用区域后平滑恢复竖直方向 | S | [ ] |
| 5-13 | 第一人称视角不会明显穿入方块 | S | [ ] |
| 5-14 | 第三人称模型不会明显穿入方块 | S | [ ] |
| 5-15 | 观察视角下模型方向正确 | S | [ ] |
| 5-16 | 空间不足时限制或暂停旋转 | D | [ ] |
| 5-17 | 不会因为旋转产生异常弹飞 | D | [ ] |
| 5-18 | 不会在墙角持续抖动 | D | [ ] |
| 5-19 | 不会因状态切换卡死玩家 | D | [ ] |
| 5-20 | 不会因退出区域残留错误方向 | D | [ ] |
| 7-1 | GitHub Actions 自动执行、正确 Java 版本、构建成功（阶段 1 已验证） | D | [x] |
| 7-2 | 构建产物包含最终模组 JAR（阶段 1 已验证上传 Artifact） | D | [x] |
| 7-3 | 无内嵌依赖，方向重力自研（不依赖外部 Gravity API） | D | [ ] |
| 7-4 | Git 仓库不包含构建缓存和本地敏感配置 | D | [ ] |
| 7-5 | 不在本设备上执行编译或运行 | S | [x] |
