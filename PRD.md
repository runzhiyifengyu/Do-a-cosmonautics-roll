# Do-a-Cosmonautics-roll PRD（产品需求）

> 开发规则、开发分工、开发阶段、任务清单与进度记录见 [DEVELOPMENT.md](DEVELOPMENT.md)。
> 本文件只描述产品目标、使用场景、功能需求与验收标准。

---

## 1. 产品目标

制作一个适用于 Minecraft NeoForge `1.21.1` 的单人模组，使玩家在高空或宇宙维度中能够配合 Do a Barrel Roll、Cosmonautics、Aeronautics 等模组进行更自然的身体旋转和表面站立。

本模组主要负责：

- 玩家身体模型旋转
- 玩家视角旋转
- 脚部接触表面的判断
- 地面、墙面、天花板之间的平滑转向
- 楼梯行走时的连续旋转
- Aeronautics 铁把手状态下的身体方向对齐
- 旋转过程中对碰撞和穿模问题进行限制

重力大小、是否无重力以及相关物理规则由其他模组决定。本模组不负责重新定义世界重力系统，而是负责玩家方向、身体姿态及与表面相关的旋转表现和行为。

---

## 2. 使用场景

### 2.1 主世界高空

当玩家碰撞箱中心的 Y 坐标大于或等于 `8000` 时启用第一版功能。

判断依据：

- 使用玩家碰撞箱中心位置
- `Y >= 8000`：进入适用区域
- `Y < 8000`：离开适用区域

### 2.2 宇宙维度

当玩家所在维度为：

```text
rocketnautics:deep_space
```

启用第一版功能。

### 2.3 离开方块后的旋转状态

在适用区域内，玩家离开方块接触后约 `0.3` 秒，进入相应的无重力/太空旋转状态。

注意：

- 计时只用于本模组的旋转状态判断。
- 重力是否存在由其他模组决定。
- 本模组不覆盖 Do a Barrel Roll 的原有翻滚效果。
- 接触脚部可站立表面后，可以进入站立或表面行走状态。

### 2.4 表面站立

本模组只检测玩家"脚部"接触的表面。

规则：

- 脚部只检测到一个明确方向的表面时，允许站立。
- 脚部同时检测到两个或多个不同方向的表面时，不判定为站立。
- 玩家身体已经旋转后，脚部检测方向也必须跟随玩家当前身体方向。
- 身体其他部位碰到墙、地面或天花板，不应单独触发站立。
- 从地面走向墙面，或从墙面走向天花板时，允许平滑旋转并继续行走。
- 一般情况下不会主动从一个表面跳转到另一个表面，只有脚部检测到新表面时才进行方向更新。

### 2.5 楼梯

所有符合 Minecraft 楼梯行为的楼梯都应支持，包括：

- 原版楼梯
- 模组添加的楼梯
- 继承或实现原版楼梯行为的楼梯

规则：

- 玩家沿楼梯正常上行时，身体应根据楼梯的实际高度和方向连续调整角度。
- 上楼梯过程中不应出现不必要的突然翻转。
- 从平面进入墙面时，应进行平滑旋转。
- 非楼梯方块暂时不使用楼梯式旋转逻辑。
- 半砖、活板门等非楼梯方块不作为楼梯处理。

### 2.6 Aeronautics 铁把手

Aeronautics 铁把手的原有交互逻辑保持不变。

铁把手原有逻辑：

- 玩家对铁把手按下右键。
- 铁把手负责将玩家带到对应位置。

本模组只负责：

- 识别玩家处于铁把手相关状态。
- 将玩家身体旋转到与铁把手长轴平行。
- 根据玩家当前位置自动选择头部和脚部方向。
- 不接管铁把手的右键交互。
- 不改变铁把手将玩家带过去的原有运动逻辑。

---

## 3. 第一版功能需求

### 3.1 适用区域判断

- [x] 实现主世界判断。
- [x] 实现碰撞箱中心 Y 坐标 `>= 8000` 判断。
- [x] 实现 `rocketnautics:deep_space` 维度判断。
- [x] 给其他维度和高度保留后续扩展接口。
- [x] 处理进入、持续处于、离开适用区域三种状态。
- [x] 处理跨越 Y=8000 边界时的状态清理。

验收：

- 主世界中心高度低于 8000 时不启用。
- 主世界中心高度达到 8000 时启用。
- `rocketnautics:deep_space` 中启用，不依赖 Y 坐标。
- 其他维度暂不启用。
- 进入和退出区域不会残留旧的旋转状态。

### 3.2 脚部表面检测

- [ ] 定义跟随玩家身体方向变化的脚部检测区域。
- [ ] 只检测脚部，不使用头部或身体其他部位作为站立依据。
- [ ] 获取脚部接触的方块碰撞面法线。
- [ ] 合并同一方向的多个方块接触。
- [ ] 区分单一方向表面和多个不同方向表面。
- [ ] 为地面、墙面、天花板提供统一方向表示。
- [ ] 处理边缘、角落、斜面和楼梯碰撞形状。

验收：

- 只接触一个方向表面时可以进入站立状态。
- 同时接触两个不同方向表面时不会错误站立。
- 身体旋转后脚部检测也随之旋转。
- 身体其他部位接触表面不会单独触发站立。

### 3.3 平滑旋转

- [ ] 实现站立方向的平滑旋转过程。
- [ ] 允许地面、墙面、天花板之间进行方向过渡。
- [ ] 防止方向在边缘位置反复抖动。
- [ ] 处理玩家快速移动和快速离开表面的情况。
- [ ] 旋转过程中保留 Do a Barrel Roll 的原有翻滚姿态。
- [ ] 明确本模组旋转与 Do a Barrel Roll 旋转的叠加顺序。
- [ ] 离开适用区域时平滑恢复原版竖直方向。

验收：

- 身体转向不能瞬间跳变。
- 从地面走到墙面时可以平滑旋转约 90 度。
- 不同方向表面之间不会持续来回切换。
- 离开适用区域后可以恢复正常朝向。
- Do a Barrel Roll 的原有翻滚不被覆盖。

### 3.4 楼梯旋转

- [ ] 识别原版和模组楼梯。
- [ ] 根据楼梯碰撞形状判断玩家当前行走进度。
- [ ] 根据楼梯方向和高度连续调整玩家身体角度。
- [ ] 区分正常上楼和进入墙面的情况。
- [ ] 防止楼梯边缘造成旋转抖动。
- [ ] 处理上下楼、倒退和横向移动。

验收：

- 原版楼梯可以正常行走。
- 模组楼梯可以正常行走。
- 上楼时角度连续变化，不出现突然翻转。
- 普通完整方块仍使用普通表面判断。
- 半砖和活板门不会被错误当作楼梯。

### 3.5 Do a Barrel Roll 兼容

- [ ] 使用 Mixin 或其他必要的兼容入口接入 Do a Barrel Roll。
- [ ] 确认实际注入点来自对应版本源码或映射。
- [ ] 保留 Do a Barrel Roll 的俯仰、偏航和翻滚效果。
- [ ] 只叠加本模组的表面方向和铁把手方向。
- [ ] 避免重复修改同一个旋转值。
- [ ] 处理模组未安装、版本不匹配或相关类不可用的情况。

验收：

- Do a Barrel Roll 已有翻滚行为仍可用。
- 本模组启用后不会覆盖其翻滚姿态。
- 旋转组合不会出现明显跳变、反向或异常锁定。
- 缺少可选兼容内容时不会导致游戏启动崩溃。

### 3.6 旋转碰撞和防穿模

- [ ] 不依赖外部 Gravity API 库（NeoForge `1.21.1` 无可用版本），方向重力自研：注入两端共有的 `LivingEntity.travel()`，把重力方向旋转到表面法线反方向。
- [ ] 重力大小由 Cosmonautics/Sable 决定，本模组只改变重力方向，不重新定义重力大小。
- [ ] 最终发布包为单个模组 JAR（无内嵌依赖）。
- [ ] 处理旋转后的玩家碰撞方向。
- [ ] 旋转前检查目标空间是否足够。
- [ ] 空间不足时限制或暂缓旋转。
- [ ] 不使用缩放模型来解决穿模。
- [ ] 处理第一人称镜头。
- [ ] 处理第三人称玩家模型。
- [ ] 处理旁观或其他观察视角下的玩家模型。
- [ ] 处理旋转时靠近方块、墙角和狭窄通道的情况。

验收：

- 旋转不会将玩家身体强制送进方块。
- 空间不足时旋转会限制或暂停。
- 第一人称镜头不会明显穿入方块。
- 第三人称身体模型方向与玩家碰撞方向一致。
- 不会因为防穿模逻辑导致玩家被异常弹飞或卡死。

### 3.7 铁把手方向对齐

- [ ] 识别 Aeronautics 铁把手。
- [ ] 读取铁把手长轴方向。
- [ ] 在铁把手原有拉近逻辑执行时叠加玩家身体旋转。
- [ ] 使身体纵轴与铁把手长轴平行。
- [ ] 根据玩家当前位置自动确定头脚方向。
- [ ] 处理铁把手状态结束。
- [ ] 防止铁把手旋转与表面站立旋转互相抢占。

验收：

- 右键铁把手的原有行为没有改变。
- 玩家被带到铁把手附近时身体方向与铁把手平行。
- 头脚方向能够根据位置自动选择。
- 铁把手状态结束后玩家可以恢复正常的旋转状态。

---

## 4. 第一版暂时不做的内容

- [ ] 多人联机支持。
- [ ] 服务端旋转状态同步。
- [ ] 服务端碰撞和反作弊校验。
- [ ] 跨玩家同步身体姿态。
- [ ] 其他维度的完整功能。
- [ ] 主世界 Y 小于 8000 时的完整功能。
- [ ] 重力大小计算。
- [ ] 无重力判定。
- [ ] 修改其他模组的重力系统。
- [ ] 修改 Do a Barrel Roll 的核心翻滚算法。
- [ ] 修改 Aeronautics 铁把手的右键、拉近和移动逻辑。
- [ ] 半砖、活板门、普通斜面等非楼梯方块的楼梯式处理。
- [ ] 复杂载具、骑乘实体和其他特殊实体的旋转适配。
- [ ] 服务端持久化玩家旋转数据。
- [ ] 配置界面。
- [ ] 自定义按键。
- [ ] 多人网络协议。
- [ ] 对所有其他旋转类模组的兼容。

---

## 5. 验收标准

> 验收分两种模式执行：
> - **标准验收模式（S）**：正常单人游戏流程中直接操作与观察验证（行走、站立、旋转、楼梯、铁把手、视角等常规场景）。
> - **Debug 验收模式（D）**：借助调试手段验证——开发日志、调试命令/可视化、构造边界场景（如 Y=8000 边界、角落、狭窄通道）、缺失依赖启动测试等，重点覆盖边界值、状态清理、异常路径与无崩溃。
> - 每项验收适用的模式见 DEVELOPMENT.md 第 10 节「PRD 检查清单」中的「验收模式」列（S / D / S+D）。

### 功能验收

- [ ] 主世界中心 Y 坐标达到 8000 时启用。
- [ ] `rocketnautics:deep_space` 中启用。
- [ ] 其他维度和主世界低于 8000 时不启用完整功能。
- [ ] 离开方块约 0.3 秒后进入对应旋转状态。
- [ ] 脚部只接触单一方向表面时可以站立。
- [ ] 脚部接触多个不同方向表面时不会站立。
- [ ] 地面、墙面和天花板之间可以平滑转向。
- [ ] 原版和模组楼梯都可以按楼梯逻辑行走。
- [ ] Do a Barrel Roll 原有翻滚功能保持不变。
- [ ] 铁把手右键和拉近逻辑保持不变。
- [ ] 铁把手状态下身体与铁把手长轴平行。
- [ ] 离开适用区域后平滑恢复竖直方向。

### 碰撞和视觉验收

- [ ] 第一人称视角不会明显穿入方块。
- [ ] 第三人称模型不会明显穿入方块。
- [ ] 观察视角下模型方向正确。
- [ ] 空间不足时限制或暂停旋转。
- [ ] 不会因为旋转产生异常弹飞。
- [ ] 不会在墙角持续抖动。
- [ ] 不会因状态切换卡死玩家。
- [ ] 不会因退出区域残留错误方向。
